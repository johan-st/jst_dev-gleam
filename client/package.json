{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:lustre": "gleam run -m lustre/dev build --minify --outdir=../server/priv/static",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-gleam": "^0.4.3"
  }
}
