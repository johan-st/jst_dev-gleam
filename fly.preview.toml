# Fly.io configuration for jst_dev preview
# Gleam/BEAM server with SQLite + LiteFS

app = 'jst-dev-preview'
primary_region = 'arn'

[env]
  PORT = '8080'
  DB_PATH = '/litefs/db.sqlite'
  ENVIRONMENT = 'preview'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[mounts]
  source = 'litefs_preview'
  destination = '/data'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
